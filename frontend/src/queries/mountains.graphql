query($distance: MinMaxInt, $position: Position, $elevation: MinMaxInt) {
  mountains(distance: $distance, position: $position, elevation: $elevation) {
    id
    name
    elevation
    distance
    country
    position {
      lat
      lng
    }
    image
  }
}