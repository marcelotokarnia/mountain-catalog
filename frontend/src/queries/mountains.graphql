query($distance: Int, $lat: Float, $lng: Float) {
  mountains(distance: $distance, lat: $lat, lng: $lng) {
    properties {
      name
      elevation
    }
    geometry {
      coordinates
    }
  }
}